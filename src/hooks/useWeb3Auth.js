import { useContext } from 'react';
import AppContext from '../contexts/app';

export const useWeb3Auth = () => {
    const connectWallet = () => {
        
    }

    return {
        isOpenTopbar,
        setIsOpenTopbar
    }
}